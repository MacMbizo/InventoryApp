<Window x:Class="KitchenInventory.Desktop.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:val="clr-namespace:KitchenInventory.Desktop.Validations"
        mc:Ignorable="d"
        Title="Settings" Height="280" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Low stock threshold:" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBox Grid.Row="0" Grid.Column="1" Width="120">
            <TextBox.Text>
                <Binding Path="LowStockThreshold" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" NotifyOnValidationError="True" ValidatesOnExceptions="True">
                    <Binding.ValidationRules>
                        <val:DoubleRangeValidationRule Min="0" Max="1000000" AllowEmpty="False"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Expiring soon (days):" VerticalAlignment="Center" Margin="0,8,8,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Width="120" Margin="0,8,0,0">
            <TextBox.Text>
                <Binding Path="ExpiringSoonDays" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" NotifyOnValidationError="True" ValidatesOnExceptions="True" />
            </TextBox.Text>
        </TextBox>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,12,0,0">
            <CheckBox Content="Enable anonymous crash reporting (helps improve app)" IsChecked="{Binding CrashReportingEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Margin="24,4,0,0" Foreground="Gray" FontSize="11" TextWrapping="Wrap" Text="Note: Changing this requires an app restart to fully apply."/>
        </StackPanel>

        <DockPanel Grid.Row="5" Grid.ColumnSpan="2" LastChildFill="False" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click" />
                <Button Content="Cancel" Width="80" IsCancel="True" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>